PROFILE=paper             # paper | testnet | live
MODE=testnet              # routing mode for order/market data
RUN_MODE=testnet
BASE_URL=http://localhost:8000
SAFE_MODE=true            # blocks live operations
DRY_RUN_ONLY=false        # if true, always simulate via paper broker
POST_ONLY=true
REDUCE_ONLY=true
TWO_MAN_RULE=true
AUTH_ENABLED=true         # require API_TOKEN for mutating API calls when true
API_TOKEN=
IDEM_TTL_SEC=60           # TTL (seconds) for in-memory idempotency cache
API_RATE_PER_MIN=30       # tokens refilled per minute for rate limiting
API_BURST=10              # maximum burst size for mutating endpoint requests
# paper/testnet keys (leave empty if not used)
BINANCE_UM_API_KEY_TESTNET=
BINANCE_UM_API_SECRET_TESTNET=
BINANCE_UM_BASE_TESTNET=https://testnet.binancefuture.com
# live keys (REAL MONEY — keep safe)
BINANCE_LV_API_KEY=
BINANCE_LV_API_SECRET=
BINANCE_LV_BASE_URL=https://fapi.binance.com
OKX_API_KEY_TESTNET=
OKX_API_SECRET_TESTNET=
OKX_API_PASSPHRASE_TESTNET=
# universe/venues
UNIVERSE=BTCUSDT,ETHUSDT
VENUES=binance-um,okx-perp
